{% if app.user %}
    <a href="{{ path('logout') }}"> <div class="myButton">Déconnexion</div></a>
{% else %}
    <a href="{{ path('login') }}">
        <div class="myButton">Connexion/Inscription</div>
    </a>
{% endif %}

<a href="{{ path('home') }}"> <div class="myButton">Catalogue</div></a>
<a href="{{ path('home') }}"> <div class="myButton">Ma liste de course</div></a>
<a href="{{ path('home') }}"> <div class="myButton">Accueil</div></a>