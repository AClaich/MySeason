<br>


<div class="d-flex justify-content-around" >
    <a href="{{ path('home') }}">
    <svg width="110" height="111">
        <circle cx="60" cy="50" r="46" stroke="#1C440C" stroke-width="1" fill-opacity="0"/>
        <circle cx="58" cy="50" r="48" stroke="#1C440C" stroke-width="2" fill-opacity="0"/>
        <circle cx="59" cy="52" r="44" stroke="#1C440C" stroke-width="1" fill-opacity="0"/>
        <circle cx="62" cy="49" r="45" stroke="#1C440C" stroke-width="2" fill-opacity="0"/>
        <text x="30" y="50"
              style="font-family: 'Zhi Mang Xing', cursive;color: #1C440C"
              font-size="20">MySeason
        </text>
        <text x="35" y="75"
              style="font-family: 'Zhi Mang Xing', cursive;color: #1C440C"
              font-size="15">Soyez Sur
        </text>
        Sorry, your browser does not support inline SVG.
    </svg>
    </a>
    <a href="{{ path('home') }}">
        <div class="myButton">Accueil</div>
    </a>
    &nbsp;
    <a href="{{ path('catalogue') }}">
        <div class="myButton">Catalogue</div>
    </a>
    &nbsp;
    <a href="{{ path('user_course') }}">
        <div class="myButton">Ma liste de course</div>
    </a>
    &nbsp;
    {% if app.user %}
        <a href="{{ path('logout') }}">
            <div class="myButton" style="">Déconnexion</div>
        </a>
        &nbsp;
    {% else %}
        <a href="{{ path('login') }}">
            <div class="myButton">Connexion/Inscription</div>
        </a>
        &nbsp;
    {% endif %}

    {% if is_granted('ROLE_SUPER_ADMIN') %}
        <a href="{{ path('admin_ajout') }}">
            <div class="myButton">Ajouter un élément</div>
        </a>
    {% endif %}

</div>
